# Scripts de Clase 6

Los scripts ejecutables de esta clase viven en la raíz del curso:

- `generate_synthetic_mortgage_dataset.py`
- `train_risk_logistic.py`
- `train_umbrella_logistic_demo.py`

Se mantienen en un solo lugar para evitar duplicar lógica entre la carpeta del curso y el proyecto NestJS.

Desde la raíz del curso puedes ejecutarlos dentro del notebook local con Docker o desde una terminal con las dependencias instaladas.
